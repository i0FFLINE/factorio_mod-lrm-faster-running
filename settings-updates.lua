data.raw["double-setting"]["lrm-faster-running-multiplier"].allowed_values = {0.1}
for i = 0.5, 10.0, 0.5 do
  table.insert(data.raw["double-setting"]["lrm-faster-running-multiplier"].allowed_values, i)
end
